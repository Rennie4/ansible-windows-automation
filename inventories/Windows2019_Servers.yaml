---
# inventory.yml

# The 'all' group is the mandatory root element for the inventory.
all:
  # The 'children' key defines groups of hosts.
  children:
    # --- Production Group ---
    windows_prod:
      hosts:
       
        10.128.235.111 :
         
          
    # --- Development Group ---
    windows_dev:
      hosts:
        
        10.128.235.112 :
          
  # The 'vars' key defines variables applied to all hosts in this inventory.
  vars:
    # Connection Details for Windows/WinRM
    ansible_connection: winrm
    ansible_port: 5986
    ansible_user: Test
    ansible_password: P@ssw0rd10
    ansible_winrm_transport: ntlm
    ansible_winrm_server_cert_validation: ignore
